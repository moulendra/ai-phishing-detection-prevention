version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./instance:/app/instance
    environment:
      - FLASK_HOST=0.0.0.0
      - SECRET_KEY=${SECRET_KEY:?Set SECRET_KEY in your environment or .env file}
